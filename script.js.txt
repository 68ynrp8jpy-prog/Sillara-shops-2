// ===== DATA =====
const SL_DISTRICTS = {
  "Colombo":["Colombo","Dehiwala","Moratuwa","Kotte","Maharagama","Piliyandala","Homagama","Kaduwela","Kolonnawa","Boralesgamuwa","Sri Jayawardenepura","Nugegoda","Ratmalana","Mount Lavinia","Kesbewa","Battaramulla"],
  "Gampaha":["Gampaha","Negombo","Ja-Ela","Wattala","Minuwangoda","Kadawatha","Kelaniya","Ragama","Kandana","Katunayake","Seeduwa","Mahabage","Ganemulla","Mirigama","Divulapitiya","Udugampola"],
  "Kalutara":["Kalutara","Panadura","Horana","Bandaragama","Matugama","Beruwala","Aluthgama","Ingiriya","Wadduwa","Palindanuwara","Agalawatta","Bulathsinhala"],
  "Kandy":["Kandy","Peradeniya","Katugastota","Gampola","Nawalapitiya","Akurana","Kundasale","Kadugannawa","Deltota","Pilimathalawa","Udadumbara","Teldeniya","Wattegama","Panvila"],
  "Matale":["Matale","Dambulla","Sigiriya","Galewela","Ukuwela","Rattota","Pallepola","Yatawatta","Nalanda","Naula"],
  "Nuwara Eliya":["Nuwara Eliya","Hatton","Talawakele","Ginigathena","Kotagala","Ragala","Ambewela","Pundaluoya","Bogawantalawa","Maskeliya"],
  "Galle":["Galle","Ambalangoda","Hikkaduwa","Elpitiya","Karapitiya","Baddegama","Imaduwa","Akmeemana","Bentota","Balapitiya","Ahangama","Koggala"],
  "Matara":["Matara","Weligama","Dikwella","Akuressa","Deniyaya","Hakmana","Kamburupitiya","Pitabeddara","Devinuwara","Kirinda Puhulwella"],
  "Hambantota":["Hambantota","Tangalle","Tissamaharama","Beliatta","Ambalantota","Mamadala","Weeraketiya","Suriyawewa","Kataragama","Lunugamwehera"],
  "Jaffna":["Jaffna","Nallur","Chavakachcheri","Point Pedro","Kopay","Manipay","Chunnakam","Kankesanthurai","Velanai","Delft"],
  "Kilinochchi":["Kilinochchi","Pallai","Paranthan","Karachchi","Poonakary"],
  "Mannar":["Mannar","Nanattan","Madhu","Musalai","Pesalai"],
  "Vavuniya":["Vavuniya","Cheddikulam","Vengalacheddikulam","Nedunkerny"],
  "Mullaitivu":["Mullaitivu","Puthukkudiyiruppu","Oddusuddan","Karuppiddi"],
  "Batticaloa":["Batticaloa","Kattankudy","Eravur","Valachchenai","Kiran","Vakarai","Pankudavely"],
  "Ampara":["Ampara","Kalmunai","Sainthamaruthu","Akkaraipattu","Sammanthurai","Pottuvil","Lahugala","Namal Oya"],
  "Trincomalee":["Trincomalee","Kinniya","Kantale","Muttur","Gomarankadawala","Seruwila"],
  "Kurunegala":["Kurunegala","Kuliyapitiya","Narammala","Pannala","Mawathagama","Giriulla","Polgahawela","Ibbagamuwa","Malkaduwawa","Nikaweratiya","Hettipola"],
  "Puttalam":["Puttalam","Chilaw","Wennappuwa","Marawila","Anamaduwa","Dankotuwa","Nattandiya","Kalpitiya","Palavi"],
  "Anuradhapura":["Anuradhapura","Kekirawa","Medawachchiya","Mihintale","Tambuttegama","Eppawala","Galenbindunuwewa","Rambewa","Kebithigollewa"],
  "Polonnaruwa":["Polonnaruwa","Kaduruwela","Medirigiriya","Hingurakgoda","Lankapura","Dimbulagala"],
  "Badulla":["Badulla","Bandarawela","Haputale","Ella","Welimada","Mahiyanganaya","Passara","Lunugala","Hali-Ela","Kandaketiya","Soranatota"],
  "Monaragala":["Monaragala","Wellawaya","Bibile","Buttala","Kataragama","Siyambalanduwa","Madulla","Sevanagala"],
  "Ratnapura":["Ratnapura","Balangoda","Embilipitiya","Pelmadulla","Eheliyagoda","Kuruwita","Nivithigala","Kolonna","Kahawatta","Godakawela"],
  "Kegalle":["Kegalle","Mawanella","Warakapola","Rambukkana","Ruwanwella","Deraniyagala","Yatiyantota","Galigamuwa","Bulathkohupitiya"]
};

const VEGETABLES = ["Bandakka (Okra)","Karawila (Bitter Gourd)","Watakolu (Ridge Gourd)","Thakkali (Tomato)","Wambatu (Eggplant)","Maalu Miris (Capsicum)","Amu Miris (Green Chili)","Bonchi (Beans)","Gowa (Cabbage)","Carrot","Leeks","Kola Carrot","Kapum (Mushroom)","Kukul Mas Wal (Drumstick)","Papol (Papaya)","Del (Breadfruit)","Kos (Jackfruit)","Labu (Ash Gourd)","Wattakka (Pumpkin)","Kankun (Water Spinach)","Mukunuwenna","Thampala","Nivithi","Radish","Beetroot","Pumpkin","Cucumber"];
const FRUITS = ["Kesel (Banana)","Papol (Papaya)","Amba (Mango)","Annasi (Pineapple)","Dodam (Orange)","Lemon","Jambu (Rose Apple)","Rambutan","Mangosteen","Udumbara (Wood Apple)","Beli (Bael)","Nelli (Amla)","Anona (Custard Apple)","Diwul (Elephant Apple)","Durian","Apple","Draksha (Grapes)","Pera (Guava)","Watermelon","Pomegranate","Avocado","Passion Fruit"];

const SHOP_EMOJIS = {vegetables:'🥬',fruits:'🍎',grocery:'🏪',milk:'🥛',garage:'🔧',phone:'📱',hardware:'🔨'};

let allShops = [];
let currentUser = null;
let currentLang = 'en';
let selectedShopType = '';
let selectedItems = [];
let shopPhotoData = '';
let ownerPhotoData = '';

// ===== SDK INIT =====
const defaultConfig = { app_title: 'Sillara Shop', background_color: '#0a0a1a', surface_color: '#1a1a3e', text_color: '#e2e8f0', primary_color: '#6366f1', secondary_color: '#8b5cf6', font_family: 'Poppins', font_size: 14 };
window.elementSdk.init({ defaultConfig, onConfigChange: async (config) => { const t = config.app_title || defaultConfig.app_title; const el = document.getElementById('nav-title'); if(el) el.textContent = t; }, mapToCapabilities: (config) => ({ recolorables: [{get:()=>config.background_color||defaultConfig.background_color,set:v=>{config.background_color=v;window.elementSdk.setConfig({background_color:v})}},{get:()=>config.surface_color||defaultConfig.surface_color,set:v=>{config.surface_color=v;window.elementSdk.setConfig({surface_color:v})}},{get:()=>config.text_color||defaultConfig.text_color,set:v=>{config.text_color=v;window.elementSdk.setConfig({text_color:v})}},{get:()=>config.primary_color||defaultConfig.primary_color,set:v=>{config.primary_color=v;window.elementSdk.setConfig({primary_color:v})}},{get:()=>config.secondary_color||defaultConfig.secondary_color,set:v=>{config.secondary_color=v;window.elementSdk.setConfig({secondary_color:v})}}], borderables:[], fontEditable:{get:()=>config.font_family||defaultConfig.font_family,set:v=>{config.font_family=v;window.elementSdk.setConfig({font_family:v})}}, fontSizeable:{get:()=>config.font_size||defaultConfig.font_size,set:v=>{config.font_size=v;window.elementSdk.setConfig({font_size:v})}} }), mapToEditPanelValues: (config) => new Map([["app_title", config.app_title || defaultConfig.app_title]]) });
const dataHandler = { onDataChanged(data) { allShops = data; renderShopList(); if(currentUser) renderMyShop(); } };
window.dataSdk.init(dataHandler);

function showToast(msg, type='success') { const c = document.getElementById('toast-container'); const t = document.createElement('div'); t.className = `toast ${type==='error'?'bg-red-600':'bg-emerald-600'}`; t.textContent = msg; c.appendChild(t); setTimeout(()=>t.remove(), 3000); }
function handleFilePreview(input, previewId) { const file = input.files[0]; if(!file) return; const reader = new FileReader(); reader.onload = (e) => { const img = document.getElementById(previewId); img.src = e.target.result; img.classList.remove('hidden'); if(previewId === 'shop-photo-preview') shopPhotoData = e.target.result; else ownerPhotoData = e.target.result; }; reader.readAsDataURL(file); }
function switchTab(tab) { document.querySelectorAll('[id^="view-"]').forEach(v=>v.classList.add('hidden')); document.getElementById('view-'+tab).classList.remove('hidden'); document.querySelectorAll('.tab-btn').forEach(b=>{ b.classList.remove('tab-active'); b.classList.add('glass','text-white/70'); }); const activeBtn = document.querySelector(`.tab-btn[data-tab="${tab}"]`); if(activeBtn){activeBtn.classList.add('tab-active');activeBtn.classList.remove('glass','text-white/70');} if(tab==='register') loadPredefinedItems(); }
function showView(v) { document.querySelectorAll('[id^="view-"]').forEach(el=>el.classList.add('hidden')); document.getElementById('view-'+v).classList.remove('hidden'); }
function populateDistricts(selId) { const sel = document.getElementById(selId); sel.innerHTML = '<option value="">-- Select District --</option>'; Object.keys(SL_DISTRICTS).sort().forEach(d => { sel.innerHTML += `<option value="${d}">${d}</option>`; }); }
function loadVillages() { const d = document.getElementById('reg-district').value; const sel = document.getElementById('reg-village'); sel.innerHTML = '<option value="">-- Select Village --</option>'; if(d && SL_DISTRICTS[d]) SL_DISTRICTS[d].forEach(v => sel.innerHTML += `<option value="${v}">${v}</option>`); }
function loadNearVillages() { const d = document.getElementById('near-district').value; const sel = document.getElementById('near-village'); sel.innerHTML = '<option value="">-- Select Village --</option>'; if(d && SL_DISTRICTS[d]) SL_DISTRICTS[d].forEach(v => sel.innerHTML += `<option value="${v}">${v}</option>`); }
function openMapPicker() { const c = document.getElementById('map-picker-container'); c.classList.toggle('hidden'); document.getElementById('map-iframe').src = 'https://www.google.com/maps/@7.8731,80.7718,8z'; }
function applyMapCoords() { const lat = document.getElementById('map-lat-input').value.trim(); const lng = document.getElementById('map-lng-input').value.trim(); if(lat && lng) { document.getElementById('reg-lat').value = lat; document.getElementById('reg-lng').value = lng; showToast('📍 Location set!'); } }
function selectShopType(type) { selectedShopType = type; document.querySelectorAll('#shop-type-selector .shop-type-card').forEach(c => { c.classList.toggle('selected', c.dataset.type === type); }); loadPredefinedItems(); }
function loadPredefinedItems() { const container = document.getElementById('predefined-items'); container.innerHTML = ''; let items = []; if(selectedShopType === 'vegetables') items = VEGETABLES; else if(selectedShopType === 'fruits') items = FRUITS; items.forEach(item => { const added = selectedItems.find(i=>i.name===item); const btn = document.createElement('button'); btn.type = 'button'; btn.className = `text-xs px-3 py-1 rounded-full transition ${added?'bg-indigo-600 text-white':'glass text-white/70 hover:bg-white/10'}`; btn.textContent = (added?'✓ ':'')+item; btn.onclick = () => { if(!added) addItem(item); }; container.appendChild(btn); }); renderItemsList(); }
function addItem(name) { if(!selectedItems.find(i=>i.name===name)) { selectedItems.push({name, price:'', discount:'', category: selectedShopType}); loadPredefinedItems(); } }
function addCustomItem() { const input = document.getElementById('custom-item-name'); const name = input.value.trim(); if(name) { addItem(name); input.value=''; } }
function removeItem(idx) { selectedItems.splice(idx,1); loadPredefinedItems(); }
function renderItemsList() { const c = document.getElementById('items-container'); c.innerHTML = ''; const vegetables = selectedItems.filter(it => it.category === 'vegetables'); const fruits = selectedItems.filter(it => it.category === 'fruits'); const other = selectedItems.filter(it => !it.category || (it.category !== 'vegetables' && it.category !== 'fruits')); if(vegetables.length) { c.innerHTML += '<p class="text-indigo-300 text-xs font-semibold mt-3 mb-2">🥬 VEGETABLES</p>'; vegetables.forEach((item, i) => { const realIdx = selectedItems.indexOf(item); c.innerHTML += `<div class="glass rounded-xl p-3 mb-2 fade-in"><div class="flex justify-between items-center mb-2"><span class="text-white text-sm font-medium">${item.name}</span><button onclick="removeItem(${realIdx})" class="text-red-400 text-xs">✕</button></div><div class="flex gap-2"><input class="input-style text-xs flex-1" placeholder="Price (LKR)" value="${item.price}" oninput="selectedItems[${realIdx}].price=this.value"><input class="input-style text-xs w-24" placeholder="Discount %" value="${item.discount}" oninput="selectedItems[${realIdx}].discount=this.value"></div></div>`; }); } if(fruits.length) { c.innerHTML += '<p class="text-emerald-300 text-xs font-semibold mt-3 mb-2">🍎 FRUITS</p>'; fruits.forEach((item, i) => { const realIdx = selectedItems.indexOf(item); c.innerHTML += `<div class="glass rounded-xl p-3 mb-2 fade-in"><div class="flex justify-between items-center mb-2"><span class="text-white text-sm font-medium">${item.name}</span><button onclick="removeItem(${realIdx})" class="text-red-400 text-xs">✕</button></div><div class="flex gap-2"><input class="input-style text-xs flex-1" placeholder="Price (LKR)" value="${item.price}" oninput="selectedItems[${realIdx}].price=this.value"><input class="input-style text-xs w-24" placeholder="Discount %" value="${item.discount}" oninput="selectedItems[${realIdx}].discount=this.value"></div></div>`; }); } if(other.length) { c.innerHTML += '<p class="text-amber-300 text-xs font-semibold mt-3 mb-2">📦 OTHER ITEMS</p>'; other.forEach((item, i) => { const realIdx = selectedItems.indexOf(item); c.innerHTML += `<div class="glass rounded-xl p-3 mb-2 fade-in"><div class="flex justify-between items-center mb-2"><span class="text-white text-sm font-medium">${item.name}</span><button onclick="removeItem(${realIdx})" class="text-red-400 text-xs">✕</button></div><div class="flex gap-2"><input class="input-style text-xs flex-1" placeholder="Price (LKR)" value="${item.price}" oninput="selectedItems[${realIdx}].price=this.value"><input class="input-style text-xs w-24" placeholder="Discount %" value="${item.discount}" oninput="selectedItems[${realIdx}].discount=this.value"></div></div>`; }); } }
async function registerShop() { const name = document.getElementById('reg-name').value.trim(); const email = document.getElementById('reg-email').value.trim(); const password = document.getElementById('reg-password').value.trim(); const phone = document.getElementById('reg-country').value + document.getElementById('reg-phone').value.trim(); const shopName = document.getElementById('reg-shop-name').value.trim(); const district = document.getElementById('reg-district').value; const village = document.getElementById('reg-village').value; const address = document.getElementById('reg-address').value.trim(); const lat = document.getElementById('reg-lat').value; const lng = document.getElementById('reg-lng').value; if(!name||!email||!password||!shopName||!selectedShopType||!district||!village) { showToast('Please fill all required fields','error'); return; } if(allShops.length >= 999) { showToast('Maximum shops reached!','error'); return; } const btn = document.getElementById('reg-submit-btn'); btn.disabled = true; btn.textContent = '⏳ Registering...'; const itemsJson = JSON.stringify(selectedItems); const result = await window.dataSdk.create({ type: 'shop', shop_name: shopName, owner_name: name, email: email, password: password, phone: phone, country: document.getElementById('reg-country').value, district: district, village: village, address: address, lat: lat, lng: lng, shop_type: selectedShopType, photo: '', owner_photo: '', items_json: itemsJson, rating_total: 0, rating_count: 0, created_at: new Date().toISOString(), owner_email: email }); btn.disabled = false; btn.textContent = '🚀 Register Shop'; if(result.isOk) { showToast('✅ Shop Registered Successfully!'); currentUser = {email, password}; document.getElementById('login-btn').classList.add('hidden'); document.getElementById('user-menu').classList.remove('hidden'); selectedItems = []; selectedShopType = ''; document.getElementById('register-form').reset(); switchTab('home'); } else { showToast('Registration failed','error'); } }
function loginUser() { const email = document.getElementById('login-email').value.trim(); const pw = document.getElementById('login-password').value.trim(); const shop = allShops.find(s=>s.email===email && s.password===pw); if(shop) { currentUser = {email, password: pw}; document.getElementById('login-btn').classList.add('hidden'); document.getElementById('user-menu').classList.remove('hidden'); showToast('✅ Welcome back, '+shop.owner_name+'!'); switchTab('home'); } else { showToast('Invalid email or password','error'); } }
function renderShopList(filter, filterDistrict, filterVillage) { let shops = allShops.filter(s=>s.type==='shop'); if(filter) shops = shops.filter(s=>s.shop_type===filter); if(filterDistrict) shops = shops.filter(s=>s.district===filterDistrict); if(filterVillage) shops = shops.filter(s=>s.village===filterVillage); const list = document.getElementById('shop-list'); const noShops = document.getElementById('no-shops'); if(shops.length === 0) { list.innerHTML = ''; noShops.classList.remove('hidden'); return; } noShops.classList.add('hidden'); list.innerHTML = shops.map((s,i) => { const emoji = SHOP_EMOJIS[s.shop_type]||'🏪'; const rating = s.rating_count > 0 ? (s.rating_total/s.rating_count).toFixed(1) : 'New'; const stars = s.rating_count > 0 ? '⭐'.repeat(Math.round(s.rating_total/s.rating_count)) : ''; let items = []; try { items = JSON.parse(s.items_json||'[]'); } catch(e){} const hasDiscount = items.some(it=>it.discount && parseFloat(it.discount)>0); return `<div class="glass rounded-2xl p-4 card-hover cursor-pointer fade-in" onclick="showShopDetail('${s.__backendId}')" style="animation-delay:${i*50}ms"><div class="flex items-start gap-3"><div class="w-14 h-14 rounded-xl bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-2xl">${emoji}</div><div class="flex-1"><div class="flex items-center gap-2"><h3 class="text-white font-bold text-sm truncate">${s.shop_name}</h3>${hasDiscount?'<span class="discount-badge">OFFERS</span>':''}</div><p class="text-white/50 text-xs">${s.owner_name} • ${s.village}, ${s.district}</p><div class="flex items-center gap-2 mt-1"><span class="text-yellow-400 text-xs">${stars} ${rating}</span><span class="text-white/30 text-xs">(${s.rating_count} ratings)</span></div><div class="mt-1 flex flex-wrap">${items.slice(0,3).map(it=>`<span class="item-chip">${it.name}</span>`).join('')}${items.length>3?`<span class="item-chip">+${items.length-3} more</span>`:''}</div></div></div></div>`; }).join(''); }
function filterByType(type) { renderShopList(type); document.getElementById('no-shops').classList.add('hidden'); }
function showShopDetail(id) { const s = allShops.find(sh=>sh.__backendId===id); if(!s) return; showView('detail'); const emoji = SHOP_EMOJIS[s.shop_type]||'🏪'; const rating = s.rating_count > 0 ? (s.rating_total/s.rating_count).toFixed(1) : '0'; let items = []; try { items = JSON.parse(s.items_json||'[]'); } catch(e){} document.getElementById('detail-content').innerHTML = `<button onclick="switchTab('home')" class="text-indigo-400 text-sm mb-3 flex items-center gap-1 hover:underline">← Back</button><div class="glass rounded-2xl p-5 slide-up"><div class="flex items-center gap-4 mb-4"><div class="w-20 h-20 rounded-2xl bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-4xl">${emoji}</div><div><h2 class="text-white font-bold text-xl">${s.shop_name}</h2><p class="text-white/60 text-sm">by ${s.owner_name}</p><p class="text-white/40 text-xs">${s.village}, ${s.district}</p><div class="flex items-center gap-1 mt-1"><span class="text-yellow-400">⭐ ${rating}</span><span class="text-white/40 text-xs">(${s.rating_count} ratings)</span></div></div></div>${s.lat && s.lng ? `<a href="https://www.google.com/maps/dir/?api=1&destination=${s.lat},${s.lng}" target="_blank" class="btn-primary w-full text-white py-2.5 rounded-xl font-semibold text-sm flex items-center justify-center gap-2 mb-4">📍 Get Directions on Map</a><iframe src="https://www.google.com/maps?q=${s.lat},${s.lng}&output=embed" class="w-full h-40 rounded-xl mb-4 border border-white/10" allowfullscreen></iframe>` : ''}<p class="text-white/40 text-xs mb-1">📞 ${s.phone||'N/A'} • 📧 ${s.email}</p><h3 class="text-white font-bold text-sm mt-4 mb-2">🛍️ Available Items</h3><div class="space-y-2">${items.map(it => { const price = it.price ? parseFloat(it.price) : 0; const disc = it.discount ? parseFloat(it.discount) : 0; const discountedPrice = disc > 0 ? price - (price * disc / 100) : price; return `<div class="glass rounded-xl p-3 flex justify-between items-center"><span class="text-white text-sm">${it.name}</span><div class="text-right">${disc > 0 ? `<span class="text-red-400 text-xs line-through">LKR ${price.toLocaleString()}</span><br>` : ''}<span class="text-emerald-400 font-bold text-sm">LKR ${discountedPrice.toLocaleString()}</span>${disc > 0 ? `<span class="discount-badge ml-1">${disc}% OFF</span>` : ''}</div></div>`; }).join('')}${items.length===0?'<p class="text-white/40 text-sm text-center py-4">No items listed yet</p>':''}</div><h3 class="text-white font-bold text-sm mt-4 mb-2">⭐ Rate This Shop</h3><div class="flex gap-1 mb-2" id="rating-stars-${id}">${[1,2,3,4,5,6].map(n=>`<button onclick="rateShop('${id}',${n})" class="star-btn text-2xl ${n<=Math.round(rating)?'text-yellow-400':'text-white/20'}">${n<=Math.round(rating)?'★':'☆'}</button>`).join('')}</div></div>`; }
async function rateShop(id, stars) { const s = allShops.find(sh=>sh.__backendId===id); if(!s) return; const result = await window.dataSdk.update({ ...s, rating_total: (s.rating_total||0) + stars, rating_count: (s.rating_count||0) + 1 }); if(result.isOk) { showToast(`Rated ${stars} stars! ⭐`); setTimeout(()=>showShopDetail(id), 500); } }
function showMyShop() { if(!currentUser) { showToast('Please login first','error'); return; } const myShops = allShops.filter(s=>s.email===currentUser.email); if(myShops.length===0) { document.getElementById('myshop-content').innerHTML = `<div class="glass rounded-2xl p-6 text-center"><p class="text-white/60">You haven't registered any shop yet.</p><button onclick="switchTab('register')" class="btn-primary mt-3 text-white py-2 px-4 rounded-xl text-sm">Register Now</button></div>`; return; } let html = `<div class="space-y-4">`; myShops.forEach(shop => { let items = []; try { items = JSON.parse(shop.items_json||'[]'); } catch(e){} const emoji = SHOP_EMOJIS[shop.shop_type]||'🏪'; const rating = shop.rating_count > 0 ? (shop.rating_total/shop.rating_count).toFixed(1) : 'New'; html += `<div class="glass rounded-2xl p-5"><div class="flex items-center gap-3 mb-4"><div class="w-16 h-16 rounded-2xl bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-3xl">${emoji}</div><div class="flex-1"><h3 class="text-white font-bold text-lg">${shop.shop_name}</h3><p class="text-white/50 text-sm">${shop.owner_name}</p><p class="text-yellow-400 text-xs">⭐ ${rating} (${shop.rating_count} ratings)</p><span class="text-emerald-400 text-xs font-semibold">✅ Registered</span></div></div><p class="text-white/50 text-xs mb-3">📍 ${shop.village}, ${shop.district} | 📞 ${shop.phone}</p><div id="shop-items-${shop.__backendId}" class="space-y-2 mb-3">${items.map((it,i)=>`<div class="glass rounded-xl p-3"><div class="flex justify-between items-center mb-2"><span class="text-white text-sm font-medium">${it.name}</span><button onclick="removeMyItem('${shop.__backendId}',${i})" class="text-red-400 text-xs">✕ Remove</button></div><div class="flex gap-2"><input class="input-style text-xs flex-1" value="${it.price||''}" placeholder="Price (LKR)" oninput="updateMyItem('${shop.__backendId}',${i},'price',this.value)"><input class="input-style text-xs w-24" value="${it.discount||''}" placeholder="Discount %" oninput="updateMyItem('${shop.__backendId}',${i},'discount',this.value)"></div></div>`).join('')}</div><div class="mt-2"><div class="flex flex-wrap gap-1 mb-2" id="pred-${shop.__backendId}"></div><div class="flex gap-2"><input class="input-style flex-1 text-xs" id="new-item-${shop.__backendId}" placeholder="New item name"><button onclick="addMyCustomItem('${shop.__backendId}')" class="btn-primary text-white text-xs px-3 rounded-xl">+ Add</button></div></div><button onclick="saveMyItems('${shop.__backendId}')" class="btn-primary w-full text-white py-2 rounded-xl font-semibold text-sm mt-3">💾 Save Changes</button><button onclick="deleteShop('${shop.__backendId}')" class="glass w-full text-red-400 py-2 rounded-xl text-sm font-medium mt-2">🗑️ Delete Shop</button></div>`; }); html += `<button onclick="logoutUser()" class="glass w-full text-red-400 py-2 rounded-xl text-sm font-medium mt-4">🚪 Logout</button></div>`; document.getElementById('myshop-content').innerHTML = html; myShops.forEach(shop => { let predContainer = document.getElementById(`pred-${shop.__backendId}`); let predItems = []; if(shop.shop_type==='vegetables') predItems = VEGETABLES; else if(shop.shop_type==='fruits') predItems = FRUITS; let existing = []; try { existing = JSON.parse(shop.items_json||'[]'); } catch(e){} predItems.forEach(pItem => { if(!existing.find(i=>i.name===pItem)) { const btn = document.createElement('button'); btn.className = 'text-xs px-2 py-1 rounded-full glass text-white/60 hover:bg-white/10 transition'; btn.textContent = '+ ' + pItem; btn.onclick = () => { let items = []; try { items = JSON.parse(shop.items_json||'[]'); } catch(e){} items.push({name:pItem,price:'',discount:''}); updateShopItems(shop, items); }; predContainer.appendChild(btn); } }); }); }
let myShopItemsCache = {};
function updateMyItem(shopId, idx, field, value) { const shop = allShops.find(s=>s.__backendId===shopId); if(!shop) return; let items = []; try { items = JSON.parse(shop.items_json||'[]'); } catch(e){} if(items[idx]) items[idx][field] = value; myShopItemsCache[shopId] = items; }
function removeMyItem(shopId, idx) { const shop = allShops.find(s=>s.__backendId===shopId); if(!shop) return; let items = []; try { items = JSON.parse(shop.items_json||'[]'); } catch(e){} items.splice(idx,1); updateShopItems(shop, items); }
function addMyCustomItem(shopId) { const input = document.getElementById(`new-item-${shopId}`); const name = input.value.trim(); if(!name) return; const shop = allShops.find(s=>s.__backendId===shopId); if(!shop) return; let items = []; try { items = JSON.parse(shop.items_json||'[]'); } catch(e){} items.push({name, price:'', discount:''}); input.value = ''; updateShopItems(shop, items); }
async function updateShopItems(shop, items) { const result = await window.dataSdk.update({...shop, items_json: JSON.stringify(items)}); if(result.isOk) { showToast('✅ Items updated!'); showMyShop(); } else showToast('Failed to update','error'); }
async function saveMyItems(shopId) { if(myShopItemsCache[shopId]) { const shop = allShops.find(s=>s.__backendId===shopId); if(shop) await updateShopItems(shop, myShopItemsCache[shopId]); delete myShopItemsCache[shopId]; } else showToast('No changes to save','error'); }
async function deleteShop(shopId) { if(confirm('Are you sure you want to delete this shop? This action is permanent.')) { const shop = allShops.find(s=>s.__backendId===shopId); if(shop) { const result = await window.dataSdk.delete(shop); if(result.isOk) { showToast('🗑️ Shop deleted'); if(currentUser && currentUser.email === shop.email) showMyShop(); else renderShopList(); } else showToast('Failed to delete','error'); } } }
function logoutUser() { currentUser = null; document.getElementById('login-btn').classList.remove('hidden'); document.getElementById('user-menu').classList.add('hidden'); showToast('Logged out'); switchTab('home'); }
function searchNearby() { const dist = document.getElementById('near-district').value; const vill = document.getElementById('near-village').value; if(!dist) { showToast('Please select a district','error'); return; } const shops = allShops.filter(s=>s.type==='shop'&&s.district===dist&&(vill?s.village===vill:true)); const c = document.getElementById('near-results'); if(shops.length===0) { c.innerHTML = '<p class="text-white/40 text-center py-8">No shops found in this area</p>'; return; } c.innerHTML = shops.map(s => { const emoji = SHOP_EMOJIS[s.shop_type]||'🏪'; const rating = s.rating_count>0?(s.rating_total/s.rating_count).toFixed(1):'New'; return `<div class="glass rounded-2xl p-4 card-hover cursor-pointer fade-in" onclick="showShopDetail('${s.__backendId}')"><div class="flex items-center gap-3"><div class="w-12 h-12 rounded-xl bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-xl">${emoji}</div><div class="flex-1"><h3 class="text-white font-bold text-sm">${s.shop_name}</h3><p class="text-white/50 text-xs">${s.owner_name} • ⭐ ${rating}</p></div>${s.lat&&s.lng?`<a href="https://www.google.com/maps/dir/?api=1&destination=${s.lat},${s.lng}" target="_blank" class="btn-primary text-white text-xs px-3 py-1.5 rounded-lg" onclick="event.stopPropagation()">📍 Directions</a>`:''}</div></div>`; }).join(''); }
function openAI() { document.getElementById('ai-modal').classList.remove('hidden'); }
function sendChat() { const input = document.getElementById('chat-input'); const msg = input.value.trim(); if(!msg) return; input.value = ''; const container = document.getElementById('chat-messages'); container.innerHTML += `<div class="chat-bubble chat-user">${msg}</div>`; const reply = getAIResponse(msg); setTimeout(()=>{ container.innerHTML += `<div class="chat-bubble chat-bot">${reply}</div>`; container.scrollTop = container.scrollHeight; }, 500); container.scrollTop = container.scrollHeight; }
function getAIResponse(msg) { const m = msg.toLowerCase(); if(m.includes('vegetable')||m.includes('එළවළු')||m.includes('recommend')||m.includes('suggest')) { const vegShops = allShops.filter(s=>s.shop_type==='vegetables'); if(vegShops.length===0) return "😔 තවම vegetable shops ගොනැහැ. පළමු ව register කරන්න! 🌱"; const bestShop = vegShops.sort((a,b)=>(b.rating_total/Math.max(b.rating_count,1))-(a.rating_total/Math.max(a.rating_count,1)))[0]; return `🥬 Best Vegetable Shop:<br><strong>${bestShop.shop_name}</strong><br>📍 ${bestShop.village}, ${bestShop.district}<br>⭐ Rating: ${bestShop.rating_count>0?(bestShop.rating_total/bestShop.rating_count).toFixed(1):'New'}<br>👉 Tap 'Home' → 'Vegetables' to see all!`; } if(m.includes('fruit')||m.includes('පළතුරු')||m.includes('apple')||m.includes('mango')) { const fruitShops = allShops.filter(s=>s.shop_type==='fruits'); if(fruitShops.length===0) return "🍎 තවම fruit shops ගොනැහැ!"; const bestShop = fruitShops.sort((a,b)=>(b.rating_total/Math.max(b.rating_count,1))-(a.rating_total/Math.max(a.rating_count,1)))[0]; return `🍎 Best Fruit Shop:<br><strong>${bestShop.shop_name}</strong><br>📍 ${bestShop.village}, ${bestShop.district}<br>⭐ Rating: ${bestShop.rating_count>0?(bestShop.rating_total/bestShop.rating_count).toFixed(1):'New'}`; } if(m.includes('best')||m.includes('top')||m.includes('excellent')||m.includes('best shops')||m.includes('ඉතුරු')) { const shops = allShops.filter(s=>s.rating_count>0).sort((a,b)=>(b.rating_total/b.rating_count)-(a.rating_total/a.rating_count)).slice(0,3); if(shops.length===0) return "⭐ තවම ratings නැහැ. Shops rate කරන්න!"; let result = "🏆 Top Rated Shops:<br>"; shops.forEach((s,i)=>result+=`${i+1}. <strong>${s.shop_name}</strong> ⭐ ${(s.rating_total/s.rating_count).toFixed(1)} (${s.rating_count} ratings)<br>`); return result; } if(m.includes('colombo')||m.includes('kandy')||m.includes('galle')||m.includes('matara')||m.includes('jaffna')) { let dist = ''; if(m.includes('colombo')) dist = 'Colombo'; else if(m.includes('kandy')) dist = 'Kandy'; else if(m.includes('galle')) dist = 'Galle'; else if(m.includes('matara')) dist = 'Matara'; else if(m.includes('jaffna')) dist = 'Jaffna'; const shops = allShops.filter(s=>s.district===dist); if(shops.length===0) return `📍 ${dist} හි තවම shops නැහැ!`; return `📍 Shops in ${dist}: ${shops.length}<br>${shops.slice(0,3).map(s=>`• ${s.shop_name} (${s.shop_type})`).join('<br>')}${shops.length>3?`<br>... සහ තවත් ${shops.length-3} shops`:''}<br>👉 'Near Me' tab එකේ සෙවීමට!`; } if(m.includes('discount')||m.includes('offer')||m.includes('ඩිස්කවුන්ට්')||m.includes('deal')||m.includes('අඩුයි')) { const discountShops = allShops.filter(s=>{ try { const items = JSON.parse(s.items_json||'[]'); return items.some(it=>it.discount && parseFloat(it.discount)>0); } catch(e) { return false; } }); if(discountShops.length===0) return "🏷️ තවම discounts නැහැ! Shops offers දාන්න! 🎉"; return `🏷️ Shops with Special Offers:<br>${discountShops.slice(0,3).map(s=>`• <strong>${s.shop_name}</strong> - ${s.shop_type}`).join('<br>')}${discountShops.length>3?`<br>... සහ තවත් ${discountShops.length-3}`:''}<br>👉 Home tab එකේ 'OFFERS' badge එක බලන්න!`; } if(m.includes('login')||m.includes('ලොගින්')||m.includes('sign in')) { if(currentUser) return `✅ ඔබ දැනටමත් logged in! Welcome ${currentUser.email}! 🎉`; return "👉 'Login' button එක ඔබන්න (nav bar එකේ)<br>👉 ඔබගේ Email සහ Password ඇතුළත් කරන්න<br>👉 Login button ඔබන්න ✅<br><br>ගිණුම නැතිද? 'Register' tab එකට යන්න!"; } if(m.includes('register')||m.includes('රෙජිස්ටර්')||m.includes('sign up')) { if(currentUser) return "ඔබ දැනටමත් registered! 'My Shop' බලන්න! 🏪"; return "👉 'Register' tab එකට යන්න<br>👉 ඔබගේ නම, Email, Password, Phone දාන්න<br>👉 Shop Name සහ Type තෝරන්න<br>👉 District & Village තෝරන්න<br>👉 Items & Prices එක add කරන්න<br>👉 Register button ඔබන්න ✅"; } if(m.includes('item')||m.includes('price')||m.includes('මිල')||m.includes('ගාන')||m.includes('add')) { if(!currentUser) return "🛍️ items දාන්නට පළමුව login/register වෙන්න!"; return "🛍️ Items & Prices හැසිරවීම:<br>👉 'My Shop' tab එකට යන්න<br>👉 Price සහ Discount % දාන්න<br>👉 Predefined items මඟින් quick add කරන්න<br>👉 Custom items එකත් දාන්න පුළුවන්<br>👉 'Save Changes' button ඔබන්න ✅"; } if(m.includes('location')||m.includes('map')||m.includes('ලොකේෂන්')||m.includes('දිස්ත්‍රික්ක')||m.includes('ගම්')) { return "📍 Location සෙවුම:<br>👉 'Near Me' tab එකට යන්න<br>👉 District සහ Village තෝරන්න<br>👉 'Search Shops' button ඔබන්න<br>👉 Nearby shops පෙන්වෙයි! 📍"; } if(m.includes('help')||m.includes('උදව්')||m.includes('how')||m.includes('කොහොමද')) { return "🤝 මට උදව් කරන්න පුළුවන්!<br>• Register / Login<br>• Items & Prices<br>• Best Shops<br>• Discounts සහ Offers<br>• Location සෙවීම<br>• Ratings දෙම<br><br>ඕනෑම දෙයක් අහන්න! 😊"; } if(m.includes('rate')||m.includes('star')||m.includes('rating')||m.includes('තරු')) { return "⭐ Rating දෙන්න:<br>👉 Shop එකක් click කරන්න<br>👉 පහළින් stars (1-6) එකට click කරන්න<br>👉 ඔබගේ rating සුරකින්න ✅<br><br>Ratings අලුත් shops rank කරයි! 🌟"; } if(m.includes('statistics')||m.includes('how many')||m.includes('කීයද')||m.includes('සංඛ්‍යා')) { const totalShops = allShops.length; const byType = {}; allShops.forEach(s=>{byType[s.shop_type]=(byType[s.shop_type]||0)+1}); return `📊 Platform Statistics:<br><strong>Total Shops:</strong> ${totalShops}<br>${Object.entries(byType).map(([t,c])=>`• ${t}: ${c}`).join('<br>')}<br><br>ඔබත් register කරන්න! 🚀`; } if(m.includes('near')||m.includes('nearby')||m.includes('close')||m.includes('අවට')) { return "📍 Nearby Shops හොයන්න:<br>👉 'Near Me' tab එකට යන්න<br>👉 ඔබගේ district තෝරන්න<br>👉 Village (optional) තෝරන්න<br>👉 'Search Shops' button ඔබන්න<br>👉 All shops with location දුන්න හොයි! 🗺️"; } return `🤖 මට තේරුණේ නැහැ! 😅<br><br>උදාහරණ:<br>• 'Register කරන්නේ කොහොමද?'<br>• 'Best shops මොනවාද?'<br>• 'Colombo හි shops'<br>• 'Discounts තියෙනවාද?'<br>• 'Nearby shops'<br>• 'How to rate?'<br><br>ස්පෙසිෆික් ප්‍රශ්නයක් අහන්න! 💬`; }
function setLang(lang) { currentLang = lang; document.getElementById('lang-btn').textContent = lang==='en'?'සිංහල':'English'; }
function showHelp() { document.getElementById('help-modal').classList.remove('hidden'); }

// ===== INITIAL RENDER VIEWS (because index.html has empty views-container) =====
function buildViews() {
  const container = document.getElementById('views-container');
  container.innerHTML = `
    <div id="view-home" class="fade-in">
      <div class="relative py-8 mb-4" style="background:linear-gradient(135deg,rgba(99,102,241,.15),rgba(139,92,246,.15));border-radius:24px;overflow:hidden"><div class="absolute inset-0 opacity-10"><div style="position:absolute;width:200px;height:200px;background:radial-gradient(circle,#6366f1);border-radius:50%;top:-50px;left:-50px"></div><div style="position:absolute;width:200px;height:200px;background:radial-gradient(circle,#8b5cf6);border-radius:50%;bottom:-50px;right:-50px"></div></div><div class="relative px-6 text-center"><h1 class="text-4xl font-black text-white mb-2" style="background:linear-gradient(135deg,#6366f1,#8b5cf6);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">🛒 Sillara Shop</h1><p class="text-white/70 text-sm">ශ්‍රී ලංකාවේ හොඳම ශොප් එක හොයා ගන්න</p><p class="text-white/50 text-xs mt-1">Find the Best Shops Near You</p></div></div>
      <h2 class="text-white font-bold text-lg mb-3">🏪 Shop Categories</h2>
      <div class="grid grid-cols-2 sm:grid-cols-3 gap-3 mb-6" id="home-categories">
        <div onclick="filterByType('vegetables')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🥬</div><div class="text-white font-semibold text-sm">Vegetables</div><div class="text-white/50 text-xs">එළවළු</div></div>
        <div onclick="filterByType('fruits')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🍎</div><div class="text-white font-semibold text-sm">Fruits</div><div class="text-white/50 text-xs">පළතුරු</div></div>
        <div onclick="filterByType('grocery')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🏪</div><div class="text-white font-semibold text-sm">Sillara Shop</div><div class="text-white/50 text-xs">සිල්ලර</div></div>
        <div onclick="filterByType('milk')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🥛</div><div class="text-white font-semibold text-sm">Milk Shop</div><div class="text-white/50 text-xs">කිරි</div></div>
        <div onclick="filterByType('garage')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🔧</div><div class="text-white font-semibold text-sm">Garage</div><div class="text-white/50 text-xs">ගැරිජ්</div></div>
        <div onclick="filterByType('phone')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">📱</div><div class="text-white font-semibold text-sm">Phone Shop</div><div class="text-white/50 text-xs">පෝන්</div></div>
        <div onclick="filterByType('hardware')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">🔨</div><div class="text-white font-semibold text-sm">Hardware</div><div class="text-white/50 text-xs">හාඩ්වෙයාර්</div></div>
        <div onclick="filterByType('other')" class="shop-type-card glass rounded-2xl p-4 text-center cursor-pointer hover:scale-105"><div class="text-3xl mb-2">✨</div><div class="text-white font-semibold text-sm">Other</div><div class="text-white/50 text-xs">වෙනත්</div></div>
      </div>
      <h2 class="text-white font-bold text-lg mb-3">📋 All Registered Shops</h2>
      <div id="shop-list" class="space-y-3"></div>
      <div id="no-shops" class="text-center text-white/40 py-12"><div class="text-5xl mb-3">🏪</div><p class="font-medium">No shops registered yet</p><p class="text-sm mt-1">Be the first to register your shop!</p></div>
    </div>
    <div id="view-near" class="hidden fade-in">
      <h2 class="text-white font-bold text-xl mt-4 mb-3">📍 Find Shops Near You</h2>
      <div class="glass rounded-2xl p-4 mb-4"><label class="text-white/70 text-sm mb-1 block">Select District</label> <select class="input-style mb-3" id="near-district" onchange="loadNearVillages()"><option value="">-- Select District --</option></select> <label class="text-white/70 text-sm mb-1 block">Select Village / Town</label> <select class="input-style mb-3" id="near-village"><option value="">-- Select Village --</option></select> <button onclick="searchNearby()" class="btn-primary w-full text-white py-2.5 rounded-xl font-semibold text-sm">🔍 Search Shops</button></div>
      <div id="near-results" class="space-y-3"></div>
    </div>
    <div id="view-register" class="hidden fade-in">
      <h2 class="text-white font-bold text-xl mt-4 mb-3">📝 Register Your Shop</h2>
      <form id="register-form" class="space-y-3" onsubmit="return false;">
        <div class="glass rounded-2xl p-4 space-y-3"><p class="text-indigo-400 font-semibold text-sm">👤 Owner Info</p><input class="input-style" id="reg-name" placeholder="Owner Name" required> <input class="input-style" type="email" id="reg-email" placeholder="Email" required> <input class="input-style" type="password" id="reg-password" placeholder="Password" required>
        <div class="flex gap-2"><select class="input-style w-1/3" id="reg-country"><option value="+94">🇱🇰 +94</option><option value="+91">🇮🇳 +91</option><option value="+1">🇺🇸 +1</option><option value="+44">🇬🇧 +44</option><option value="+61">🇦🇺 +61</option><option value="+971">🇦🇪 +971</option><option value="+966">🇸🇦 +966</option><option value="+82">🇰🇷 +82</option><option value="+81">🇯🇵 +81</option><option value="+39">🇮🇹 +39</option><option value="+49">🇩🇪 +49</option><option value="+33">🇫🇷 +33</option><option value="+86">🇨🇳 +86</option><option value="+7">🇷🇺 +7</option><option value="+55">🇧🇷 +55</option><option value="+60">🇲🇾 +60</option><option value="+65">🇸🇬 +65</option><option value="+66">🇹🇭 +66</option><option value="+63">🇵🇭 +63</option><option value="+92">🇵🇰 +92</option><option value="+880">🇧🇩 +880</option><option value="+977">🇳🇵 +977</option></select> <input class="input-style flex-1" id="reg-phone" placeholder="Phone Number" type="tel"></div>
        <div><label class="text-white/60 text-xs block mb-1">Owner Photo</label> <input type="file" accept="image/*" id="reg-owner-photo" class="input-style text-sm" onchange="handleFilePreview(this,'owner-photo-preview')"> <img id="owner-photo-preview" class="hidden w-16 h-16 rounded-full object-cover mt-2 border-2 border-indigo-500"></div></div>
        <div class="glass rounded-2xl p-4 space-y-3"><p class="text-indigo-400 font-semibold text-sm">🏪 Shop Info</p><input class="input-style" id="reg-shop-name" placeholder="Shop Name" required> <label class="text-white/60 text-xs block">Shop Type</label>
        <div class="grid grid-cols-3 gap-2" id="shop-type-selector"><div onclick="selectShopType('vegetables')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="vegetables">🥬 Vegetables</div><div onclick="selectShopType('fruits')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="fruits">🍎 Fruits</div><div onclick="selectShopType('grocery')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="grocery">🏪 Sillara</div><div onclick="selectShopType('milk')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="milk">🥛 Milk</div><div onclick="selectShopType('garage')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="garage">🔧 Garage</div><div onclick="selectShopType('phone')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="phone">📱 Phone</div><div onclick="selectShopType('hardware')" class="shop-type-card glass rounded-xl p-2 text-center text-xs text-white" data-type="hardware">🔨 Hardware</div></div>
        <div><label class="text-white/60 text-xs block mb-1">Shop Photo</label> <input type="file" accept="image/*" id="reg-shop-photo" class="input-style text-sm" onchange="handleFilePreview(this,'shop-photo-preview')"> <img id="shop-photo-preview" class="hidden w-full h-32 rounded-xl object-cover mt-2 border-2 border-indigo-500"></div></div>
        <div class="glass rounded-2xl p-4 space-y-3"><p class="text-indigo-400 font-semibold text-sm">📍 Location</p><select class="input-style" id="reg-district" onchange="loadVillages()"><option value="">-- Select District --</option></select> <select class="input-style" id="reg-village"><option value="">-- Select Village / Town --</option></select> <input class="input-style" id="reg-address" placeholder="Address">
        <div class="flex gap-2"><input class="input-style flex-1" id="reg-lat" placeholder="Latitude" readonly> <input class="input-style flex-1" id="reg-lng" placeholder="Longitude" readonly></div><button type="button" onclick="openMapPicker()" class="glass w-full text-indigo-400 py-2 rounded-xl text-sm font-medium hover:bg-white/10 transition">📍 Pick Location on Map</button>
        <div id="map-picker-container" class="hidden mt-2"><p class="text-white/50 text-xs mb-1">Click on the map to pick your shop location</p><iframe id="map-iframe" class="w-full h-48 rounded-xl border border-white/10" src="" allowfullscreen></iframe><div class="flex gap-2 mt-2"><input class="input-style flex-1" id="map-lat-input" placeholder="Paste Latitude"> <input class="input-style flex-1" id="map-lng-input" placeholder="Paste Longitude"> <button onclick="applyMapCoords()" class="btn-primary text-white text-xs px-3 rounded-xl">Set</button></div><p class="text-white/40 text-xs mt-1">💡 Open Google Maps, right-click your location → copy coordinates, paste above</p></div></div>
        <div class="glass rounded-2xl p-4 space-y-3" id="items-section"><p class="text-indigo-400 font-semibold text-sm">🛍️ Items & Prices</p><div id="items-container"></div><div id="predefined-items" class="flex flex-wrap gap-1 mb-2"></div><div class="flex gap-2"><input class="input-style flex-1" id="custom-item-name" placeholder="Other item name"> <button type="button" onclick="addCustomItem()" class="btn-primary text-white text-xs px-3 rounded-xl">+ Add</button></div></div>
        <button type="button" onclick="registerShop()" id="reg-submit-btn" class="btn-primary w-full text-white py-3 rounded-2xl font-bold text-base">🚀 Register Shop</button>
      </form>
    </div>
    <div id="view-login" class="hidden fade-in"><div class="max-w-sm mx-auto mt-12"><div class="glass rounded-2xl p-6 space-y-4 slide-up"><div class="text-center"><div class="text-4xl mb-2">🔐</div><h2 class="text-white font-bold text-xl">Login</h2><p class="text-white/50 text-sm">Enter your registered email & password</p></div><input class="input-style" type="email" id="login-email" placeholder="Email"> <input class="input-style" type="password" id="login-password" placeholder="Password"> <button onclick="loginUser()" class="btn-primary w-full text-white py-2.5 rounded-xl font-semibold">Login</button><p class="text-center text-white/40 text-sm">Don't have an account? <a onclick="switchTab('register')" class="text-indigo-400 cursor-pointer">Register</a></p></div></div></div>
    <div id="view-myshop" class="hidden fade-in"><h2 class="text-white font-bold text-xl mt-4 mb-3">🏪 My Shop Dashboard</h2><div id="myshop-content"></div></div>
    <div id="view-detail" class="hidden fade-in"><div id="detail-content"></div></div>
    <div id="help-modal" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/60" onclick="if(event.target===this)this.classList.add('hidden')"><div class="glass rounded-2xl p-6 max-w-md w-full mx-4 max-h-[80%] overflow-y-auto slide-up"><div class="flex justify-between items-center mb-4"><h3 class="text-white font-bold text-lg">📖 How to Register</h3><button onclick="document.getElementById('help-modal').classList.add('hidden')" class="text-white/50 hover:text-white">✕</button></div><div class="text-white/80 text-sm space-y-3"><p class="font-semibold text-indigo-400">🇱🇰 සිංහල උපදෙස්:</p><ol class="list-decimal pl-5 space-y-2"><li><strong>Register Tab</strong> එක ක්ලික් කරන්න</li><li>ඔබේ <strong>නම, Email, Password, Phone</strong> දාන්න</li><li>ඔබේ <strong>Shop Name</strong> සහ <strong>Shop Type</strong> තෝරන්න</li><li><strong>District</strong> එක තෝරන්න → <strong>Village/Town</strong> තෝරන්න</li><li><strong>Location</strong> දාන්න - Google Maps වලින්: <ul class="list-disc pl-5 mt-1 text-white/60"><li>Google Maps Open කරන්න</li><li>ඔබේ shop එක තියෙන තැන right-click කරන්න</li><li>Coordinates copy කරන්න (eg: 6.0535, 80.2210)</li><li>"Pick Location on Map" ක්ලික් කරල paste කරන්න</li></ul></li><li>ඔබේ <strong>Items & Prices</strong> add කරන්න (LKR වලින්)</li><li><strong>Discount</strong> දාන්න ඕනම් percentage එක දාන්න</li><li><strong>Register Shop</strong> button එක ක්ලික් කරන්න ✅</li></ol><p class="text-indigo-400 font-semibold mt-4">🔑 Login වෙන්න:</p><ol class="list-decimal pl-5 space-y-1"><li><strong>Login</strong> button එක ක්ලික් කරන්න</li><li>Register කරද්දි දුන් <strong>Email & Password</strong> දාන්න</li><li>Login උනාම <strong>My Shop</strong> button එකෙන් ඔබේ shop manage කරන්න</li></ol></div></div></div>
    <div id="ai-modal" class="hidden fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-black/60" onclick="if(event.target===this)this.classList.add('hidden')"><div class="glass rounded-t-2xl sm:rounded-2xl p-4 w-full sm:max-w-md sm:mx-4 max-h-[70%] flex flex-col slide-up"><div class="flex justify-between items-center mb-3"><h3 class="text-white font-bold">🤖 AI Assistant</h3><button onclick="document.getElementById('ai-modal').classList.add('hidden')" class="text-white/50 hover:text-white">✕</button></div><div id="chat-messages" class="flex-1 overflow-y-auto flex flex-col gap-1 mb-3 min-h-[200px]"><div class="chat-bubble chat-bot">Hello! 👋 I'm the Sillara Shop AI Assistant. How can I help you? <br> මට ඔබට උදව් කරන්න පුළුවන්!</div></div><div class="flex gap-2"><input class="input-style flex-1" id="chat-input" placeholder="Ask me anything..." onkeydown="if(event.key==='Enter')sendChat()"> <button onclick="sendChat()" class="btn-primary text-white px-4 rounded-xl"><i data-lucide="send" style="width:16px;height:16px"></i></button></div></div></div>
  `;
  populateDistricts('reg-district');
  populateDistricts('near-district');
  lucide.createIcons();
}

buildViews();